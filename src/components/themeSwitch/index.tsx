//import { useThemeStore } from '@/store/theme'

const DarkLightSwitch = () => {
  //   const { layoutMode, splitMenus } = useThemeStore(
  //     useShallow((state) => ({
  //       layoutMode: state.layout,
  //       splitMenus: state.splitMenus,
  //     })),
  //   )

  return <div></div>
}

export default DarkLightSwitch
